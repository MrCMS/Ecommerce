using MrCMS.Web.Apps.Ecommerce.Services.Pricing;
using MrCMS.Website;

namespace MrCMS.Web.Apps.Ecommerce.Helpers.Pricing
{
    /*public static class ProductPricingExtensions
    {
        public static decimal ProductPriceIncludingTax(this decimal amount, decimal taxRatePercentage)
        {
            return GetService().GetPriceIncludingTax(amount, taxRatePercentage);
        }

        public static decimal ProductPriceExcludingTax(this decimal amount, decimal taxRatePercentage)
        {
            return GetService().GetPriceExcludingTax(amount, taxRatePercentage);
        }

        public static decimal ProductTax(this decimal amount, decimal taxRatePercentage)
        {
            return GetService().GetTax(amount, taxRatePercentage);
        }

        public static decimal ProductPriceIncludingTax(this decimal? amount, decimal taxRatePercentage)
        {
            return GetService().GetPriceIncludingTax(amount, taxRatePercentage);
        }

        public static decimal ProductPriceExcludingTax(this decimal? amount, decimal taxRatePercentage)
        {
            return GetService().GetPriceExcludingTax(amount, taxRatePercentage);
        }

        public static decimal ProductTax(this decimal? amount, decimal taxRatePercentage)
        {
            return GetService().GetTax(amount, taxRatePercentage);
        }

        private static IProductPricingService GetService()
        {
            return MrCMSApplication.Get<IProductPricingService>();
        }
    }*/
}
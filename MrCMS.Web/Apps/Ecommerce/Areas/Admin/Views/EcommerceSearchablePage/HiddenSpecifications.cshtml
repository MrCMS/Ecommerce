@model MrCMS.Web.Apps.Ecommerce.Pages.EcommerceSearchablePage

<div id="hidden-specifications">
    <div class="row">
        <div class="col-lg-6">
            <h4>Hidden Search Specifications</h4>
        </div>
        <div class="col-lg-6">
            <div class="btn-group pull-right">
                @Html.ActionLink("Add", "AddSpecification", "EcommerceSearchablePage", new { id = Model.Id }, new { @class = "btn btn-xs btn-default", data_toggle = "fb-modal" })
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            @if (Model.HiddenSearchSpecifications.Any())
            {
                <table class="table table-striped table-condensed">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var attribute in Model.HiddenSearchSpecifications)
                        {
                            <tr>
                                <td>@attribute.Name</td>
                                <td>@Html.ActionLink("Remove", "RemoveSpecification", "EcommerceSearchablePage", new { categoryId = Model.Id, id = attribute.Id }, new { @class = "btn btn-danger btn-xs pull-right", data_action = "remove-searchable-page-hidden-specification" })</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>Please click add to hide a specification.</p>
            }
        </div>
    </div>
</div>

@model MrCMS.Web.Apps.Ecommerce.Entities.NewsletterBuilder.Newsletter
@{ 
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";
}
@using (Html.BeginForm("Delete", "Newsletter", FormMethod.Post))
{
    <fieldset>
        <legend>Delete Newsletter
            <span class="btn-group  display-inline  pull-right">
                <input type="submit" class="btn btn-danger" value="Delete Newsletter" />
            </span>
        </legend>
        @Html.HiddenFor(x => x.Id)
        <p>
            Are you sure you want to delete the newsletter @Model.Name? Deleting this item can not be undone.
        </p>
    </fieldset>
}
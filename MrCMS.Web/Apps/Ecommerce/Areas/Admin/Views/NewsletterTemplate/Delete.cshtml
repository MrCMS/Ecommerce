@model MrCMS.Web.Apps.Ecommerce.Entities.NewsletterBuilder.NewsletterTemplate
@{ 
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";
}
@using (Html.BeginForm("Delete", "NewsletterTemplate", FormMethod.Post))
{
    <fieldset>
        <legend>Delete Newsletter Template
            <span class="btn-group  display-inline  pull-right">
                <input type="submit" class="btn btn-danger" value="Delete Newsletter Template" />
            </span>
        </legend>
        @Html.HiddenFor(x => x.Id)
        <p>
            Are you sure you want to delete the currency @Model.Name? Deleting this item can not be undone.
        </p>
    </fieldset>
}
@using MrCMS.Helpers
@model MrCMS.Paging.IPagedList<MrCMS.Web.Apps.Ecommerce.Entities.Products.ProductVariant>
@if (Model.Any())
{
    <div id="skus" data-paging-type="async">
        <div class="row">
            <div class="col-xs-12">
                <table class="table table-striped table-condensed pull-left" style="margin-bottom: 0;">
                    @foreach (var item in Model)
                    {
                        <tr><td>@item.Name - @item.Product.PublishOn.GetValueOrDefault().ToString("d MMM yyyy")</td><td><div class="pull-right"><button data-sku-id="@item.SKU" data-sku-name="@item.Name" class="btn btn-success add-sku">Add</button></div></td></tr>
                    }
                </table>
            </div>
        </div>
        @Html.Pager(Model, "SearchSKUs", i => new { page = i })
    </div>
}
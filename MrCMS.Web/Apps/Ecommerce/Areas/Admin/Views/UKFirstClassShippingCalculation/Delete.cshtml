@model MrCMS.Web.Apps.Ecommerce.Entities.Shipping.UKFirstClassShippingCalculation
@{ Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";}
@using (Html.BeginForm("Delete", "UKFirstClassShippingCalculation", FormMethod.Post))
{
    <fieldset>
        <legend>
            Delete Country
            <span class="btn-group display-inline pull-right">
                <input type="submit" class="btn-primary btn" value="Delete" />
            </span>
        </legend>

        @Html.HiddenFor(x => x.Id)

        <p>
            Are you sure you want to delete this calculation? Deleting it can not be undone.
        </p>
    </fieldset>
}
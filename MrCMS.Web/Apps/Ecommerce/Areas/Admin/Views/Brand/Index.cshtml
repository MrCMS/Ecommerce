@using MrCMS.Helpers
@model MrCMS.Paging.IPagedList<MrCMS.Web.Apps.Ecommerce.Entities.Products.Brand>
@{
    ViewBag.Title = "Brands";
}

@{ Html.IncludeScript("~/Apps/Ecommerce/Areas/Admin/Content/Scripts/custom/brands.js");}

<form>
    <fieldset>
        <legend>Brands
            <span class="btn-group  display-inline  pull-right">
                <a href="@Url.Action("Add", "Brand")" class="btn btn-success"  data-toggle="fb-modal">Add Brand</a>
            </span>
        </legend>

        <div class="row-fluid">
            @using (Html.BeginForm("Index", "Brand", FormMethod.Get))
            {
                <input type="text" class="search-query" placeholder="Search" id="searchParam" name="q" value="@ViewData["query"]" />
                <input type="submit" class="btn btn-primary btn-mini" value="Search" />
            }
        </div>
        <hr />
        @if (Model.Count == 0)
        {
            @:No brands.
            }
        else
        {
            <table class="table table-condensed table-striped">
                <thead>
                    <tr>
                        <th>Name
                        </th>
                        <th>Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Name
                            </td>
                            <td>
                                <div class="btn-group">
                                    @Html.ActionLink("Edit", "Edit", "Brand", new { id = item.Id }, new { @class = "btn btn-mini", data_toggle = "fb-modal" })
                                    @Html.ActionLink("Delete", "Delete", "Brand", new { id = item.Id }, new { @class = "btn btn-mini btn-danger", data_toggle = "fb-modal" })
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </fieldset>
</form>

@Html.Pager(Model, "Index", i => new { page = i })
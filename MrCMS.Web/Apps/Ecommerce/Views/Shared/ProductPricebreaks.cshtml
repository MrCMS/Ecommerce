@using MrCMS.Web.Apps.Ecommerce.Entities.Products
@using MrCMS.Web.Apps.Ecommerce.Helpers
@model MrCMS.Web.Apps.Ecommerce.Entities.Products.ProductVariant
<div id="product-variants-pricebreaks">
    @if (Model.PriceBreaks.Any())
    {
        <div class="product-pricebreaks-title">
            PRICE BREAKS - The more you buy, the more you save.
        </div>
        <table id="product-pricebreaks-table" class="table table-condensed table-striped table-bordered">
            <thead>
                <tr>
                    <th>Quantity</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.PriceBreaks)
                {
                    <tr>
                        <td>@item.Quantity&nbsp;+</td>
                        <td>@item.Price.ToCurrencyFormat()</td>
                    </tr> 
                }
            </tbody>
        </table>
    }
</div>

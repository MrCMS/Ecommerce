@using MrCMS.Helpers
@using MrCMS.Web.Apps.Ecommerce.Entities.Users
@using MrCMS.Web.Apps.Ecommerce.Helpers
@model MrCMS.Web.Apps.Ecommerce.Pages.UserAccountAddresses      
@{
    var addresses = ViewData["addresses"] as IList<Address> ?? new List<Address>();

    Html.IncludeCss("~/Areas/Admin/Content/Styles/featherlight.css");
    Html.IncludeScript("~/Areas/Admin/Content/Scripts/lib/featherlight.js");
    Html.IncludeScript("~/Apps/Ecommerce/Areas/Admin/Content/Scripts/custom/address-popup.js");
}
<h1>@Editable(Model, page => page.Name)</h1>
@Editable(Model, page => page.BodyContent, true)
@{Html.RenderPartial("ShowMessages");}
@if (addresses.Any())
{
    foreach (var chunk in addresses.Chunk(3))
    {
        <div class="row">
            @foreach (var address in chunk)
            {
                <div class="col-sm-4">
                    <ul class="list-unstyled">
                        <li>@address.Name</li>
                        <li>@address.Company</li>
                        <li>@address.PhoneNumber</li>
                        <li>@address.Address1</li>
                        <li>@address.Address2</li>
                        <li>@address.City</li>
                        <li>@address.StateProvince</li>
                        <li>@address.PostalCode</li>
                        <li>@address.GetCountryName()</li>
                    </ul>
                    @Html.ActionLink("Edit", "EditAddress", "UserAccountAddresses", new { id = address.Id }, new { @class = "btn btn-default btn-xs", data_toggle = "fb-modal" })
                    @Html.ActionLink("Delete", "DeleteAddress", "UserAccountAddresses", new { id = address.Id }, new { @class = "btn btn-danger btn-xs", data_toggle = "fb-modal" })
                </div>
            }
        </div>
    }
}
else
{
    <p>@Resource("User Account Addresses - No Addresses", "No addresses.")</p>
}
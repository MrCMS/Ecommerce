@using MrCMS.Helpers
@model MrCMS.Web.Apps.NewsletterBuilder.Entities.TemplateData.FreeTextTemplateData

@using (Html.BeginForm("Edit", "FreeTextTemplateData", FormMethod.Post))
{
    @Html.ReturnToReferrer()
    <fieldset>
        <legend>
            Update Free Text Template Data for @Model.NewsletterTemplate.Name
            <span class="pull-right display-inline btn-group">
                <button type="submit" class="btn btn-primary">Save</button>
            </span>
        </legend>
        <div class="form-group">
            @Html.LabelFor(x => x.FreeTextTemplate)
            @Html.TextAreaFor(x => x.FreeTextTemplate, new { @class = "form-control", rows = 6 })
            @Html.ValidationMessageFor(x => x.FreeTextTemplate)
        </div>
        <div class="alert alert-info">
            <strong>Available Tokens: </strong> [Text]
        </div>
    </fieldset>
}
@model MrCMS.Web.Apps.Amazon.Models.AmazonDashboardModel

<div class="row-fluid">
        <div class="span4">
            @{Html.RenderPartial("RevenueStats",Model);}
            @{Html.RenderPartial("Revenue",Model);}
        </div>
        <div class="span4">
            @{Html.RenderPartial("ProductsSoldStats",Model);}
            @{Html.RenderPartial("ProductsSold",Model);}
        </div>
        <div class="span4">
            @{Html.RenderPartial("AppStats", Model);}
            @{Html.RenderAction("Logs", "App");}
        </div>
    </div>
@model MrCMS.Web.Apps.Ecommerce.Models.ProductPagedList
@using MrCMS.Helpers

<div id="products-wrapper">
@if (Model.Any())
{
    <div id="TotalItemCount" style="display:none">@Model.TotalItemCount</div>
    <div id="PageSize" style="display:none">@Model.Count</div>
    <div id="PageNumber" style="display:none">@Model.PageNumber</div>
    <div class="clearfix"></div>
    <div class="row-fluid">
    <div id="products">
            @foreach (var item in Model)
            {
                <div class="item">
                    <div class="span3 photo">
                    @if(item.Images.Any())
                    {
                            <img src="@(item.Images.First().FileUrl)" class="img-rounded pull-left span12" />
                    }
                    </div>
                    <div class="span6">
                        <h4><a href="/@item.LiveUrlSegment">@item.Name</a></h4>
                        @(item.Abstract!=null?(item.Abstract.Length>255?item.Abstract.Substring(0,250)+"...":item.Abstract):String.Empty)
                    </div>
                    <div class="span3">
                        <div class="pull-right">
                        Exclusive price
                            
                                        @if (item.Variants.Count>0)
                                        {
                                            <h1 class="product-price">
                                            @Html.Raw("&pound;"+item.Variants.OrderBy(x=>x.Price).First().Price.ToString("#.##"))
                                                </h1>
                                            <span>shop price </span><span style="text-decoration:line-through">&pound;@(item.Variants.OrderBy(x=>x.Price).First().Price.ToString("#.##"))</span>
                                        }
                            
                            <br /><br />
                            <a href="/@item.LiveUrlSegment" class="btn btn-info span12">
                            More info
                            </a>
                            </div>
                        </div>
                    <div class="clearfix"></div>
                </div>
            }
    </div>
    </div>
      <div id="products-paging">
        @Html.Pager(Model, "Category", i => new { pageNo = i })
    </div>
   
}
</div>
<script>
    $('#TotalItemCount').change();
    $('#PageCount').change();
    $('#PageNumber').change();
</script>

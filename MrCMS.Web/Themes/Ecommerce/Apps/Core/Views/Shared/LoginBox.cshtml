@using MrCMS.Helpers
@using MrCMS.Web.Apps.Core.Pages
@model MrCMS.Web.Apps.Core.Models.LoginModel 
@using (Html.BeginForm())
{
    <div class="row-fluid">
        <div class="span12">
            <div class="control-group">
                <label class="control-label" for="Email">Email</label>
                <div class="controls">
                    @Html.TextBoxFor(model => model.Email, new { @class = "span3"})
                    @Html.ValidationMessageFor(model => model.Email)
                </div>
            </div>
            <div class="control-group">
                <label class="control-label" for="Password">
                    Password</label>
                <div class="controls">
                    @Html.PasswordFor(model => model.Password, new { @class = "span3"})
                    @Html.ValidationMessageFor(model => model.Password)
                </div>
            </div>
            <div class="control-group">
                <div class="controls">
                    <label class="checkbox">
                        @Html.CheckBoxFor(model => model.RememberMe)
                        Remember me
                    </label>
                    <input type="submit" value="Login" class="generic-button big-red-gradient span3" />
                </div>
            </div>
            <div class="control-group">
                <div class="controls">
                    <a href="@(UniquePageHelper.GetUrl<ForgottenPasswordPage>())">Forgotten password?</a>
                </div>
            </div>

            @if (!String.IsNullOrEmpty(Model.Message))
            {
                <div class="alert alert-error">
                    @Model.Message
                </div>
            }

        </div>
    </div>
}
